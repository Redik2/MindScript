package compilers.codeParts.loopsCodeParts;

import compilers.codeParts.CodePart;

public class WhileCycle  extends CodePart {
    protected WhileCycle(int compiled_space) {
        super(compiled_space);
    }
}
